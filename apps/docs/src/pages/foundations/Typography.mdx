import {
  TallyFontPreview,
  TallySizeScale,
  TallyWeights,
} from '../../components/foundations/TallyFoundation';

<PageHeader
  title="Typography"
  description="Typography tokens collected from tally-ui-source on 2026-07-21."
/>

<div className="my-6 rounded-lg border border-default bg-muted/40 p-4 text-sm text-default">
  <p className="font-semibold text-subtle">Provenance</p>
  <p className="mt-1 text-subtle">
    These tokens were collected from tally-ui-source on 2026-07-21 and are
    this repo's token source. Values
    are rendered straight from <code>src/data/tally-foundation.json</code> via
    inline styles.
  </p>
</div>

## Font family

Tally uses **Geist** with Inter and system fallbacks.

<TallyFontPreview />

**No mono token is shipped by decision** (2026-07-21): neither Geist Mono nor
Space Mono. Mono usages (code snippets, job numbers, JSON logs) fall back to the
platform monospace stack via Tailwind's default `font-mono`, which needs no token.

## Size scale

Seven sizes in the collected scale (`text-3xs` → `text-body-lg`), of which three
— `text-xs`, `text-body`, `text-body-lg` — equal Tailwind stock values and are
marked as stock context rows. The collected line-heights **override** Tailwind's
stock `leading-tight` / `leading-snug`.

<TallySizeScale />

## Weights

<TallyWeights />

These are Tailwind stock classes as used across the Tally components
(`font-medium` 500 is the workhorse); they are documented here, not yet tokens.

## Root font-size — "THE ONE KNOB"

Root font-size is fixed at **16px**. All component sizing is authored in rem
against it, so the whole UI scales from that single value.

## Source of truth

Values on this page come from `apps/docs/src/data/tally-foundation.json`, copied
unchanged from the original collection (`design-system-testing-abdillah/foundation/tokens.json`,
v0.3.1). This collection is this repo's token source.
