import { TallySpacingScale } from '../../components/foundations/TallyFoundation';

<PageHeader
  title="Spacing"
  description="Spacing conventions observed in the Tally apps during the 2026-07-21 foundation collection."
/>

<div className="my-6 rounded-lg border border-default bg-muted/40 p-4 text-sm text-default">
  <p className="font-semibold text-subtle">Provenance</p>
  <p className="mt-1 text-subtle">
    These tokens were collected from tally-ui-source on 2026-07-21 and are
    this repo's token source.
  </p>
</div>

## Spacing scale

Tally defines no custom spacing tokens — both apps use Tailwind's default
4px-based scale. This is that scale:

<TallySpacingScale />

## The rem sizing model

Component dimensions (panel heights, input heights, icon sizes) are authored in
rem against the fixed **16px root font-size — "THE ONE KNOB"** — so the whole UI
scales proportionally from that single value. See
[Typography](/foundations/typography) for the type tokens
that share this model.

## Fixed component dimensions

Fixed dimensions (e.g. panel heights, sidebar width `15rem`) are not tokens —
they are documented per-component in the collection's component inventories. If
the design system wants a spacing scale later, that is a new decision, not an
extraction.

## Source of truth

Findings summarized from the external collection
(`design-system-testing-abdillah/foundation/foundation.md` §5 and
`tokens.json` → `spacing.note`). The JSON states `"custom": false` — there is no
spacing data to render on this page; the scale above is Tailwind's documented
default, shown for reference.
