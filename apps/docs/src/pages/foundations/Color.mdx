import {
  TallyColorRamp,
  TallySemanticColors,
} from '../../components/foundations/TallyFoundation';

<PageHeader
  title="Color"
  description="Color tokens collected from tally-ui-source plus the cool-gray ramp on 2026-07-21."
/>

<div className="my-6 rounded-lg border border-default bg-muted/40 p-4 text-sm text-default">
  <p className="font-semibold text-subtle">Provenance</p>
  <p className="mt-1 text-subtle">
    These tokens were collected from tally-ui-source on 2026-07-21 and are
    this repo's token source. Values
    are rendered straight from <code>src/data/tally-foundation.json</code> via
    inline styles.
  </p>
</div>

## Palette ramps

Six ramps extracted value-for-value from the running apps. `neutral` is the warm
brand ramp (cream/charcoal), `crimson` is the brand/action ramp (500 `#C0180C` =
primary buttons), and `red` is deliberately distinct from crimson for error states.
The cool `gray` ramp comes from Figma Primitives (18 shades incl. the 75/150/250/350/975
half-steps) — only the 3001 app had it tokenized.

### Neutral (warm)

<TallyColorRamp ramp="neutral" />

### Crimson (brand)

<TallyColorRamp ramp="crimson" />

### Green (success)

<TallyColorRamp ramp="green" />

### Amber (warning)

<TallyColorRamp ramp="amber" />

### Red (error)

<TallyColorRamp ramp="red" />

### Gray (cool)

<TallyColorRamp ramp="gray" />

## Semantic colors

<TallySemanticColors />

Notes from the collection doc:

- `secondary` is a **cool gray inside a warm system** — intentional, matches Figma.
- `border-hairline` exists, but most components actually use `border-black/10` at
  0.5px instead of the hairline token.
- `muted-foreground` / `subtle-foreground` are rarely used in components so far.

## Decisions applied 2026-07-21

1. **`--primary: #000000` → renamed to `--text-primary`** — it is a text color.
   The black was always the primary *text* color (~70 usages of the `text-primary`
   class), not the action color. Actions are crimson-500. The `--color-primary`
   theme mapping now points at `--text-primary`, so existing `text-primary`
   classes keep working.
2. **`--danger-foreground: #9C1122` → removed**, replaced with
   `rgba(255, 255, 255, 0.95)` (near-white alpha). The token is currently unused
   in app code, so this is a safe default for future danger buttons.
3. `primary-hover` (`#98150B`, crimson-600) was **kept** — it is the text hover
   state (`hover:text-primary-hover` in JobCard/JobCardV2).
4. `primary-foreground` (`#FFFFFF`) was **kept** — text on dark/primary surfaces
   (used by dashboard widgets).

## Hardcoded colors left out of this round

Per the 2026-07-21 review, promoting these to tokens was **declined for now** —
they stay hardcoded in component code and are documented here instead:

| Hex | Used for |
|---|---|
| `#4169D6` | job status "In-progress", chart series |
| `#DB4C86` | job status "Potential" |
| `#0072E4` | Actuals bars, skills level 5 |
| `#00A97F` | Calendar bars, budget "on track" |
| `#0D76F2` | map pins (staffed) |
| `#EB6214` | skills level 2 |
| `#BC97F7` / `#F7F1FF` / `#7635D9` | badge `purple` variant |
| `#2D251F` | progress-bar default fill |
| `#A2A19A` / `#D3D2CF` | gantt bar default/disabled |
| `#211D1A` | chart tooltip background |

## Source of truth

Values on this page come from `apps/docs/src/data/tally-foundation.json`, copied
unchanged from the original collection (`design-system-testing-abdillah/foundation/tokens.json`,
v0.2.0). See that repo's `foundation.md` for the full decision log. This
collection is this repo's token source.
