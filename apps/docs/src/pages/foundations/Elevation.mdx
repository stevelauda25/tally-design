import { TallyShadowGrid } from '../../components/foundations/TallyFoundation';

<PageHeader
  title="Elevation"
  description="Shadow tokens collected from tally-ui-source on 2026-07-21."
/>

<div className="my-6 rounded-lg border border-default bg-muted/40 p-4 text-sm text-default">
  <p className="font-semibold text-subtle">Provenance</p>
  <p className="mt-1 text-subtle">
    These tokens were collected from tally-ui-source on 2026-07-21 and are
    this repo's token source. Values
    are rendered straight from <code>src/data/tally-foundation.json</code> via
    inline styles.
  </p>
</div>

## Token shadows

Six token shadows: three surface elevations (`elev-1`, `elev-2`, `pop`) using the
warm charcoal `rgba(32,27,24,…)` base, and three component shadows registered
2026-07-21 (`button`, `frame`, `card`).

<TallyShadowGrid />

| Token | Use |
|---|---|
| `shadow-elev-1` | cards |
| `shadow-elev-2` | raised cards |
| `shadow-pop` | modals |
| `shadow-button` | button, segmented pill, date-picker Apply (6 layers incl. inset highlights) |
| `shadow-frame` | dashboard frame surface — the #F7F7F7 container (3 layers + inset hairlines) |
| `shadow-card` | white cards/panels inside the frame — kpi-card, dashboard panels, job cards (5 layers) |

## Documented shadow recipes (not tokens)

Two recipes remain in component code, documented here for reference:

| Recipe | Used by |
|---|---|
| Popover/menu shadow (3 layers, all `rgba(0,0,0,0.05)`) | dropdowns, menus, date-picker card |
| Chart tooltip shadow (6 layers) | chart-tooltip |
| Chart tooltip shadow (6 layers) | chart-tooltip |

## Source of truth

Values on this page come from `apps/docs/src/data/tally-foundation.json`, copied
unchanged from the original collection (`design-system-testing-abdillah/foundation/tokens.json`,
v0.2.0); recipes documented from `foundation.md` §3. This collection is this
repo's token source.
