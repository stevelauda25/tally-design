import { SegmentedButton } from '@tally-ui/ui';

export const options = [
  { value: 'week', label: 'Week', count: 12 },
  { value: 'month', label: 'Month', count: 4 },
  { value: 'quarter', label: 'Quarter', count: 1 },
];

<PageHeader
  title="Segmented Button"
  description="Single-select button group with sliding selection pill."
/>

## Small

<PreviewCard>
  <SegmentedButton size="small" options={options} value="week" onChange={() => {}} />
</PreviewCard>

## Medium

<PreviewCard>
  <SegmentedButton size="medium" options={options} value="week" onChange={() => {}} />
</PreviewCard>

## Fill

<PreviewCard>
  <SegmentedButton fill options={options} value="week" onChange={() => {}} className="w-full max-w-md" />
</PreviewCard>

## No dividers

<PreviewCard>
  <SegmentedButton dividers={false} options={options} value="week" onChange={() => {}} />
</PreviewCard>
