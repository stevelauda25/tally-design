import { Tooltip, Button } from '@tally-ui/ui';

<PageHeader
  title="Tooltip"
  description="Contextual help on hover/focus with four sides."
/>

## Sides

<PreviewCard noClip>
  <div className="flex flex-wrap gap-6">
    <Tooltip side="top" body="Top tooltip">
      <Button variant="outline" size="sm">Top</Button>
    </Tooltip>
    <Tooltip side="right" body="Right tooltip">
      <Button variant="outline" size="sm">Right</Button>
    </Tooltip>
    <Tooltip side="bottom" body="Bottom tooltip">
      <Button variant="outline" size="sm">Bottom</Button>
    </Tooltip>
    <Tooltip side="left" body="Left tooltip">
      <Button variant="outline" size="sm">Left</Button>
    </Tooltip>
  </div>
</PreviewCard>

## With title

<PreviewCard noClip>
  <Tooltip title="Heads up" body="This action cannot be undone.">
    <Button variant="outline" size="sm">Hover me</Button>
  </Tooltip>
</PreviewCard>

## Controlled open

<PreviewCard noClip>
  <Tooltip body="Always visible for demo" open>
    <Button variant="outline" size="sm">Pinned open</Button>
  </Tooltip>
</PreviewCard>
